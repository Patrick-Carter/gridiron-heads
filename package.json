{
  "name": "gridiron-heads",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -k -n server,client -c blue,magenta \"npm -w @gridiron/server run dev\" \"npm -w @gridiron/client run dev\"",
    "build": "npm -w @gridiron/shared run build && npm -w @gridiron/server run build && npm -w @gridiron/client run build",
    "start": "node server/dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc -b shared server client",
    "smoke": "node scripts/browser-smoke.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "playwright-core": "1.55",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
